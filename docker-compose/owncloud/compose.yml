services:

  owncloud:
    image: owncloud/server:latest
    container_name: owncloud
    ports:
      - ${OWNCLOUD_PORT}:8080
    depends_on:
      - db
      - redis
    environment:
      - OWNCLOUD_DOMAIN=${OWNCLOUD_DOMAIN}
      - OWNCLOUD_TRUSTED_DOMAINS=${OWNCLOUD_TRUSTED_DOMAINS}
      - OWNCLOUD_DB_TYPE=mysql
      - OWNCLOUD_DB_NAME=${OWNCLOUD_DB_NAME}
      - OWNCLOUD_DB_USERNAME=${OWNCLOUD_DB_USERNAME}
      - OWNCLOUD_DB_PASSWORD=${OWNCLOUD_DB_PASSWORD}
      - OWNCLOUD_DB_HOST=db
      - OWNCLOUD_ADMIN_USERNAME=${OWNCLOUD_ADMIN_USERNAME}
      - OWNCLOUD_ADMIN_PASSWORD=${OWNCLOUD_ADMIN_PASSWORD}
      - OWNCLOUD_MYSQL_UTF8MB4=true
      - OWNCLOUD_REDIS_ENABLED=true
      - OWNCLOUD_REDIS_HOST=redis
      - files_external_allow_create_new_local=true
    volumes:
      - ${OWNCLOUD_DIR}:/mnt/data
    restart: unless-stopped

  db:
    image: mariadb:latest
    container_name: own_mariadb
    restart: unless-stopped
    environment:
      - MYSQL_DATABASE=${OWNCLOUD_DB_NAME}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_USER=${OWNCLOUD_DB_USERNAME}
      - MYSQL_PASSWORD=${OWNCLOUD_DB_PASSWORD}
    command: ["--max-allowed-packet=128M", "--innodb-log-file-size=64M"]      
    volumes:
      - ${CONFIG_DIR}/.owncloud/mysql:/var/lib/mysql

  redis:
    image: redis:latest
    container_name: own_redis
    restart: unless-stopped
    command: ["--databases", "1"]
    volumes:
      - ${CONFIG_DIR}/.owncloud/redis:/data
