# canon driver keywords; quality is auto-managed per media type
media=4x6
MediaType=glossygold
# the Canon PPD ships with a grayscale ColorModel default; pin color
# or every photo silently prints black & white
print-color-mode=color
