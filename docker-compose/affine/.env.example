# port the server container exposes
PORT=3010

CONFIG_DIR=/srv/dev-disk-by-uuid-a36653d0-9781-4de1-b5ab-5275818d484c/mediacenter/configs

# database credentials (postgres is internal-only, not exposed)
DB_USERNAME=affine
DB_PASSWORD=affine
DB_DATABASE=affine
